# Enghacks-project

## AI 注释说明

本项目使用如下 AI 标注约定：

- `AI_GENERATED_START` / `AI_GENERATED_END`
  - 用于标记 AI 生成或 AI 主导生成的代码区块。
  - 被标记区块内部不再额外添加普通解释注释。

前端 UI 相关文件（HTML/CSS）统一使用 `AI_GENERATED` 文件头标注。

README 不维护函数级别的详细标注清单；演示时以 skeleton 和设计思路为主，按需再展开具体函数实现。
